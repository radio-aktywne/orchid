import { identity } from "./vars/identity";
import { localization } from "./vars/localization";
import { test } from "./vars/test";

export const orpcProcedures = {
  identity: identity,
  localization: localization,
  test: test,
};
