export const constants = {
  nodes: {
    csrf: "csrf_token",
    displayName: "traits.names.display",
    domain: "upstream_parameters.hd",
    preferredLocale: "traits.locales.preferred",
    profilePictureUrl: "traits.pictures.profile.url",
    prompt: "upstream_parameters.prompt",
    provider: "provider",
  },
} as const;
